#ifndef UTILS_H
#define UTILS_H

#include <string>
#include <vector>

// Função para calcular um hash simples da mensagem
std::string calculateHash(const std::string& input);

// Função para verificar se um número é primo
bool isPrime(int num);

#endif // UTILS_H 